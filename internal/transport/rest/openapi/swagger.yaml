definitions:
  domain.ComputeAttributes:
    properties:
      family:
        type: string
      ram_gb:
        type: number
      vcpu:
        type: number
    type: object
  domain.DatabaseNoSQLAttributes:
    properties:
      component_type:
        description: '"throughput", "storage", "request_operations"'
        type: string
      data_model:
        description: '"document", "key_value", "wide_column", "graph", "multi_model"'
        type: string
      multi_region:
        description: Multi-region replication / global distribution
        type: boolean
      pricing_mode:
        description: '"provisioned", "on_demand", "serverless"'
        type: string
      read_units:
        description: Provisioned reads/sec (RCU/RU/s) or request rate
        type: number
      replication_zones:
        description: 'Number of replica regions (default: 1)'
        type: integer
      storage_class:
        description: '"standard", "infrequent_access", "analytical"'
        type: string
      storage_gb:
        description: Storage capacity in GiB
        type: number
      write_units:
        description: Provisioned writes/sec (WCU/RU/s) or request rate
        type: number
    type: object
  domain.DatabaseRDBMSAttributes:
    properties:
      component_type:
        description: '"instance" or "storage"'
        type: string
      deployment_tier:
        description: '"standard", "flexible", "burstable", "aurora"'
        type: string
      engine:
        description: '"postgresql", "mysql", "sqlserver", "mariadb", "oracle"'
        type: string
      iops:
        description: Provisioned IOPS
        type: integer
      multi_az:
        description: High Availability deployment
        type: boolean
      ram_gb:
        description: Memory capacity in GiB (0 for pure storage rows)
        type: number
      storage_family:
        description: '"gp3", "gp2", "io1", "ssd", "hdd"'
        type: string
      storage_gb:
        description: Storage capacity in GiB (0 for pure instance rows)
        type: number
      vcpu:
        description: Number of vCPUs (0 for pure storage rows)
        type: number
    type: object
  domain.KubernetesAttributes:
    properties:
      cluster_topology:
        description: '"zonal", "regional", "autopilot" — GCP-specific'
        type: string
      tier:
        description: '"free", "standard", "extended_support"'
        type: string
    type: object
  domain.NetworkAttributes:
    properties:
      egress_gb:
        type: number
      transfer_type:
        type: string
    type: object
  domain.ServerlessRateAttributes:
    properties:
      architecture:
        description: '"x86_64", "arm64"'
        type: string
      component_type:
        description: '"request_fee", "duration_fee", "duration_fee_cpu", "duration_fee_memory"'
        type: string
      tier:
        description: '"consumption", "flex_consumption", "1st_gen", "2nd_gen"'
        type: string
      unit:
        description: '"per_million_requests", "per_request", "per_10_requests", "per_gb_second",
          etc.'
        type: string
    type: object
  domain.StorageAttributes:
    properties:
      iops:
        description: |-
          IOPS holds provisioned IOPS where reported (e.g. block storage EBS gp3/io2).
          Currently unpopulated for S3/Blob/GCS object storage in stage 1.4.
        type: integer
      size_gb:
        type: number
      storage_class:
        type: string
    type: object
  middleware.RFC7807Error:
    properties:
      detail:
        type: string
      instance:
        type: string
      status:
        type: integer
      title:
        type: string
      type:
        type: string
    type: object
  rest.CalculateCategoryResult:
    properties:
      match_delta_pct:
        type: number
      match_quality:
        type: string
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.CalculateMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      request:
        $ref: '#/definitions/rest.CalculateRequestBody'
    type: object
  rest.CalculateProviderResult:
    properties:
      categories:
        additionalProperties:
          $ref: '#/definitions/rest.CalculateCategoryResult'
        type: object
      partial:
        type: boolean
      partial_total_normalized_hourly_usd:
        type: number
      provider:
        type: string
      total_normalized_hourly_usd:
        type: number
    type: object
  rest.CalculateRequestBody:
    properties:
      compute:
        $ref: '#/definitions/domain.ComputeAttributes'
      currency:
        type: string
      database:
        $ref: '#/definitions/domain.DatabaseRDBMSAttributes'
        description: alias for database_rdbms
      database_nosql:
        $ref: '#/definitions/domain.DatabaseNoSQLAttributes'
      database_rdbms:
        $ref: '#/definitions/domain.DatabaseRDBMSAttributes'
      kubernetes:
        $ref: '#/definitions/domain.KubernetesAttributes'
      network:
        $ref: '#/definitions/domain.NetworkAttributes'
      region:
        type: string
      serverless:
        $ref: '#/definitions/rest.ServerlessWorkloadPayload'
      storage:
        $ref: '#/definitions/domain.StorageAttributes'
      strict_family:
        type: boolean
    type: object
  rest.CalculateResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.CalculateMeta'
      results:
        items:
          $ref: '#/definitions/rest.CalculateProviderResult'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.CategoryStatusResponse:
    properties:
      category:
        example: compute
        type: string
      dlq:
        $ref: '#/definitions/rest.DLQStatusResponse'
      last_fetched_at:
        type: string
      last_seen_at:
        type: string
      observation_count:
        example: 450
        type: integer
      stale:
        example: false
        type: boolean
      staleness_threshold_hours:
        example: 168
        type: number
      supported:
        example: true
        type: boolean
    type: object
  rest.ComputeComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.ComputeComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.ComputeComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.ComputeResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.ComputeResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.ComputeAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.DLQStatusResponse:
    properties:
      consecutive_failures:
        example: 3
        type: integer
      last_error:
        example: 503 Service Unavailable
        type: string
      status:
        example: failed
        type: string
      timestamp:
        type: string
    type: object
  rest.DatabaseComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.DatabaseComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.DatabaseComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.DatabaseResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.DatabaseNoSQLComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.DatabaseNoSQLComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.DatabaseNoSQLComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.DatabaseNoSQLResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.DatabaseNoSQLResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.DatabaseNoSQLAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.DatabaseResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.DatabaseRDBMSAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.KubernetesComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.KubernetesComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.KubernetesComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.KubernetesResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.KubernetesResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.KubernetesAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.LoginRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  rest.LoginResponse:
    properties:
      access_token:
        type: string
      expires_in:
        type: integer
      refresh_token:
        type: string
      token_type:
        type: string
    type: object
  rest.LogoutRequest:
    properties:
      refresh_token:
        type: string
    type: object
  rest.LogoutResponse:
    properties:
      message:
        type: string
    type: object
  rest.NetworkComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.NetworkComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.NetworkComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.NetworkResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.NetworkResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.NetworkAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      monthly_cost_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.PriceDetail:
    properties:
      amount:
        type: number
      currency:
        type: string
      unit:
        type: string
    type: object
  rest.ProviderStatusResponse:
    properties:
      categories:
        additionalProperties:
          $ref: '#/definitions/rest.CategoryStatusResponse'
        type: object
      last_successful_fetch:
        type: string
      provider:
        example: aws
        type: string
      stale:
        example: false
        type: boolean
      status:
        example: healthy
        type: string
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderStatusWarningResponse'
        type: array
    type: object
  rest.ProviderStatusWarningResponse:
    properties:
      code:
        example: not_yet_ingested
        type: string
      message:
        example: Oracle OCI ingestion lands in stage 4.
        type: string
      provider:
        example: oracle
        type: string
    type: object
  rest.ProviderWarning:
    properties:
      code:
        type: string
      message:
        type: string
      provider:
        type: string
    type: object
  rest.RefreshRequest:
    properties:
      idempotency_key:
        type: string
      refresh_token:
        type: string
    type: object
  rest.RefreshResponse:
    properties:
      access_token:
        type: string
      expires_in:
        type: integer
      refresh_token:
        type: string
      token_type:
        type: string
    type: object
  rest.ServerlessComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.ServerlessComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.ServerlessComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.ServerlessResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.ServerlessResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.ServerlessRateAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      normalized_monthly_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.ServerlessWorkloadPayload:
    properties:
      architecture:
        type: string
      execution_duration_ms:
        type: number
      memory_mb:
        type: number
      requests_per_month:
        type: number
      tier:
        type: string
    type: object
  rest.SignupRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  rest.SignupResponse:
    properties:
      created_at:
        type: string
      email:
        type: string
      id:
        type: string
    type: object
  rest.StorageComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.StorageComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.StorageComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.StorageResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.StorageResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.StorageAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      monthly_cost_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
info:
  contact: {}
paths:
  /api/v1/auth/login:
    post:
      consumes:
      - application/json
      description: Validates credentials with timing-safe comparison and issues JWT
        access token and opaque refresh token pair
      parameters:
      - description: Login credentials
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.LoginRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "200":
          description: Authentication successful
          schema:
            $ref: '#/definitions/rest.LoginResponse'
        "400":
          description: Malformed JSON request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "401":
          description: Invalid email or password
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Rate limit exceeded
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Authenticate user and issue tokens
      tags:
      - Auth
  /api/v1/auth/logout:
    post:
      consumes:
      - application/json
      description: Revokes the specified refresh token and purges it from the rotation
        cache
      parameters:
      - description: Logout parameters
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.LogoutRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "200":
          description: Session revoked successfully
          schema:
            $ref: '#/definitions/rest.LogoutResponse'
        "400":
          description: Invalid or missing refresh_token
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Revoke session / Logout
      tags:
      - Auth
  /api/v1/auth/refresh:
    post:
      consumes:
      - application/json
      description: Rotates a valid refresh token, minting a new access and refresh
        token pair with idempotency-key gated replay and whole-family theft containment
      parameters:
      - description: Refresh token parameters
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.RefreshRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "200":
          description: Tokens rotated successfully
          schema:
            $ref: '#/definitions/rest.RefreshResponse'
        "400":
          description: Invalid or missing refresh_token or idempotency_key
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "401":
          description: Invalid, expired, revoked, or compromised token family
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Rotate refresh token
      tags:
      - Auth
  /api/v1/auth/signup:
    post:
      consumes:
      - application/json
      description: Creates a new user account with email and password
      parameters:
      - description: Signup parameters
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.SignupRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "201":
          description: User registered successfully
          schema:
            $ref: '#/definitions/rest.SignupResponse'
        "400":
          description: Invalid email or password format
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "409":
          description: User with this email already exists
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Register a new user
      tags:
      - Auth
  /api/v1/calculate:
    post:
      consumes:
      - application/json
      description: Calculates composite pricing across cloud providers for requested
        compute, storage, network, database, NoSQL, Kubernetes, and serverless specs.
      parameters:
      - description: Composite workload request payload
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/rest.CalculateRequestBody'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.CalculateResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get composite pricing calculation
      tags:
      - calculator
  /api/v1/prices/compute:
    get:
      description: Returns normalized compute pricing across cloud providers for requested
        specs.
      parameters:
      - description: Requested vCPU count (e.g. 4)
        in: query
        name: vcpu
        type: number
      - description: Requested RAM in GB (e.g. 16)
        in: query
        name: ram_gb
        type: number
      - description: Instance family filter (e.g. t3, c5)
        in: query
        name: family
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      - description: 'Strict instance family matching (default: true)'
        in: query
        name: strict_family
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.ComputeComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get compute pricing comparison
      tags:
      - prices
  /api/v1/prices/database:
    get:
      description: Returns normalized relational database pricing across cloud providers
        (AWS, Azure, GCP) for requested specs.
      parameters:
      - description: Requested database engine (e.g. postgresql, mysql, sqlserver)
        in: query
        name: engine
        type: string
      - description: Requested vCPU count (e.g. 4)
        in: query
        name: vcpu
        type: number
      - description: Requested RAM in GB (e.g. 16)
        in: query
        name: ram_gb
        type: number
      - description: Requested storage in GB (e.g. 100)
        in: query
        name: storage_gb
        type: number
      - description: Requested provisioned IOPS (e.g. 3000)
        in: query
        name: iops
        type: integer
      - description: 'High Availability / Multi-AZ deployment (default: false)'
        in: query
        name: multi_az
        type: boolean
      - description: Storage family preference (e.g. gp3, ssd, io1)
        in: query
        name: storage_family
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.DatabaseComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get relational database pricing comparison
      tags:
      - prices
  /api/v1/prices/database-nosql:
    get:
      description: Returns normalized NoSQL database pricing across cloud providers
        (AWS DynamoDB, Azure Cosmos DB, GCP Firestore) for requested specs.
      parameters:
      - description: Requested NoSQL data model (e.g. document, key_value, wide_column,
          graph)
        in: query
        name: data_model
        type: string
      - description: 'Requested pricing mode (provisioned, on_demand, serverless;
          default: provisioned)'
        in: query
        name: pricing_mode
        type: string
      - description: Requested reads per second (e.g. 100)
        in: query
        name: read_units
        type: number
      - description: Requested writes per second (e.g. 20)
        in: query
        name: write_units
        type: number
      - description: Requested storage in GB (e.g. 50)
        in: query
        name: storage_gb
        type: number
      - description: Storage class preference (e.g. standard, infrequent_access, analytical)
        in: query
        name: storage_class
        type: string
      - description: 'High Availability / Multi-Region replication (default: false)'
        in: query
        name: multi_region
        type: boolean
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.DatabaseNoSQLComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get NoSQL database pricing comparison
      tags:
      - prices
  /api/v1/prices/kubernetes:
    get:
      description: Returns normalized Kubernetes control-plane pricing across cloud
        providers (AWS EKS, Azure AKS, GCP GKE) for requested tier.
      parameters:
      - description: 'Requested Kubernetes cluster tier (free, standard, extended_support;
          default: standard)'
        in: query
        name: tier
        type: string
      - description: 'GCP-specific cluster topology for credit application (zonal,
          regional, autopilot; default: zonal)'
        in: query
        name: cluster_topology
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.KubernetesComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get Kubernetes control-plane pricing comparison
      tags:
      - prices
  /api/v1/prices/network:
    get:
      description: Returns normalized network / data transfer egress pricing across
        cloud providers (AWS, Azure, GCP) for requested specs.
      parameters:
      - description: Requested network egress in GB (e.g. 500, max 10000000)
        in: query
        name: egress_gb
        type: string
      - description: Canonical transfer type (intra_region, inter_region, internet_egress)
        in: query
        name: transfer_type
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.NetworkComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get network pricing comparison
      tags:
      - prices
  /api/v1/prices/serverless:
    get:
      description: Returns normalized Serverless compute (FaaS) pricing across cloud
        providers (AWS Lambda, Azure Functions, GCP Cloud Functions) for requested
        workload.
      parameters:
      - description: 'Requested CPU architecture (x86_64, arm64; default: x86_64)'
        in: query
        name: architecture
        type: string
      - description: 'Requested serverless tier (consumption, flex_consumption, 1st_gen,
          2nd_gen; default: consumption)'
        in: query
        name: tier
        type: string
      - description: 'Monthly invocation requests (default: 1000000)'
        in: query
        name: requests_per_month
        type: number
      - description: 'Function allocated memory in MB (128 - 10240, default: 512)'
        in: query
        name: memory_mb
        type: number
      - description: 'Average execution duration in ms (1 - 900000, default: 200)'
        in: query
        name: execution_duration_ms
        type: number
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.ServerlessComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get Serverless compute pricing comparison
      tags:
      - prices
  /api/v1/prices/storage:
    get:
      description: Returns normalized storage pricing across cloud providers (AWS,
        Azure, GCP) for requested specs.
      parameters:
      - description: Requested storage size in GB (e.g. 500, max 1000000)
        in: query
        name: size_gb
        type: string
      - description: Requested canonical storage class (e.g. standard, infrequent_access,
          archive)
        in: query
        name: storage_class
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.StorageComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get storage pricing comparison
      tags:
      - prices
  /api/v1/providers/{provider}/status:
    get:
      description: Returns the latest successful fetch timestamp, per-category observation
        count, staleness flag, and active DLQ failure records for the specified cloud
        provider.
      parameters:
      - description: Cloud provider identifier (e.g. aws, azure, gcp, oracle, ibm,
          alibaba, digitalocean)
        in: path
        name: provider
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Provider status and category freshness details
          schema:
            $ref: '#/definitions/rest.ProviderStatusResponse'
        "400":
          description: Invalid Parameters
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "404":
          description: Provider Not Found
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get cloud provider operational status and data freshness
      tags:
      - providers
swagger: "2.0"
