definitions:
  domain.ComputeAttributes:
    properties:
      family:
        type: string
      ram_gb:
        type: number
      vcpu:
        type: number
    type: object
  domain.NetworkAttributes:
    properties:
      egress_gb:
        type: number
      transfer_type:
        type: string
    type: object
  domain.StorageAttributes:
    properties:
      iops:
        description: |-
          IOPS holds provisioned IOPS where reported (e.g. block storage EBS gp3/io2).
          Currently unpopulated for S3/Blob/GCS object storage in stage 1.4.
        type: integer
      size_gb:
        type: number
      storage_class:
        type: string
    type: object
  middleware.RFC7807Error:
    properties:
      detail:
        type: string
      instance:
        type: string
      status:
        type: integer
      title:
        type: string
      type:
        type: string
    type: object
  rest.CalculateCategoryResult:
    properties:
      match_delta_pct:
        type: number
      match_quality:
        type: string
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.CalculateMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      request:
        $ref: '#/definitions/rest.CalculateRequestBody'
    type: object
  rest.CalculateProviderResult:
    properties:
      categories:
        additionalProperties:
          $ref: '#/definitions/rest.CalculateCategoryResult'
        type: object
      partial:
        type: boolean
      partial_total_normalized_hourly_usd:
        type: number
      provider:
        type: string
      total_normalized_hourly_usd:
        type: number
    type: object
  rest.CalculateRequestBody:
    properties:
      compute:
        $ref: '#/definitions/domain.ComputeAttributes'
      currency:
        type: string
      network:
        $ref: '#/definitions/domain.NetworkAttributes'
      region:
        type: string
      storage:
        $ref: '#/definitions/domain.StorageAttributes'
      strict_family:
        type: boolean
    type: object
  rest.CalculateResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.CalculateMeta'
      results:
        items:
          $ref: '#/definitions/rest.CalculateProviderResult'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.ComputeComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.ComputeComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.ComputeComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.ComputeResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.ComputeResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.ComputeAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      normalized_hourly_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.LoginRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  rest.LoginResponse:
    properties:
      access_token:
        type: string
      expires_in:
        type: integer
      refresh_token:
        type: string
      token_type:
        type: string
    type: object
  rest.LogoutRequest:
    properties:
      refresh_token:
        type: string
    type: object
  rest.LogoutResponse:
    properties:
      message:
        type: string
    type: object
  rest.NetworkComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.NetworkComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.NetworkComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.NetworkResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.NetworkResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.NetworkAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      monthly_cost_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
  rest.PriceDetail:
    properties:
      amount:
        type: number
      currency:
        type: string
      unit:
        type: string
    type: object
  rest.ProviderWarning:
    properties:
      code:
        type: string
      message:
        type: string
      provider:
        type: string
    type: object
  rest.RefreshRequest:
    properties:
      idempotency_key:
        type: string
      refresh_token:
        type: string
    type: object
  rest.RefreshResponse:
    properties:
      access_token:
        type: string
      expires_in:
        type: integer
      refresh_token:
        type: string
      token_type:
        type: string
    type: object
  rest.SignupRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  rest.SignupResponse:
    properties:
      created_at:
        type: string
      email:
        type: string
      id:
        type: string
    type: object
  rest.StorageComparisonMeta:
    properties:
      api_version:
        type: string
      generated_at:
        type: string
      query:
        additionalProperties: true
        type: object
    type: object
  rest.StorageComparisonResponse:
    properties:
      meta:
        $ref: '#/definitions/rest.StorageComparisonMeta'
      results:
        items:
          $ref: '#/definitions/rest.StorageResultEntry'
        type: array
      warnings:
        items:
          $ref: '#/definitions/rest.ProviderWarning'
        type: array
    type: object
  rest.StorageResultEntry:
    properties:
      fetched_at:
        type: string
      match_delta_pct:
        type: number
      match_quality:
        type: string
      matched_spec:
        $ref: '#/definitions/domain.StorageAttributes'
      missing_attributes:
        items:
          type: string
        type: array
      monthly_cost_usd:
        type: number
      price:
        $ref: '#/definitions/rest.PriceDetail'
      provider:
        type: string
      sku_id:
        type: string
      stale:
        type: boolean
    type: object
info:
  contact: {}
paths:
  /api/v1/auth/login:
    post:
      consumes:
      - application/json
      description: Validates credentials with timing-safe comparison and issues JWT
        access token and opaque refresh token pair
      parameters:
      - description: Login credentials
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.LoginRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "200":
          description: Authentication successful
          schema:
            $ref: '#/definitions/rest.LoginResponse'
        "400":
          description: Malformed JSON request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "401":
          description: Invalid email or password
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Rate limit exceeded
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Authenticate user and issue tokens
      tags:
      - Auth
  /api/v1/auth/logout:
    post:
      consumes:
      - application/json
      description: Revokes the specified refresh token and purges it from the rotation
        cache
      parameters:
      - description: Logout parameters
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.LogoutRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "200":
          description: Session revoked successfully
          schema:
            $ref: '#/definitions/rest.LogoutResponse'
        "400":
          description: Invalid or missing refresh_token
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Revoke session / Logout
      tags:
      - Auth
  /api/v1/auth/refresh:
    post:
      consumes:
      - application/json
      description: Rotates a valid refresh token, minting a new access and refresh
        token pair with idempotency-key gated replay and whole-family theft containment
      parameters:
      - description: Refresh token parameters
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.RefreshRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "200":
          description: Tokens rotated successfully
          schema:
            $ref: '#/definitions/rest.RefreshResponse'
        "400":
          description: Invalid or missing refresh_token or idempotency_key
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "401":
          description: Invalid, expired, revoked, or compromised token family
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Rotate refresh token
      tags:
      - Auth
  /api/v1/auth/signup:
    post:
      consumes:
      - application/json
      description: Creates a new user account with email and password
      parameters:
      - description: Signup parameters
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/rest.SignupRequest'
      produces:
      - application/json
      - application/problem+json
      responses:
        "201":
          description: User registered successfully
          schema:
            $ref: '#/definitions/rest.SignupResponse'
        "400":
          description: Invalid email or password format
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "405":
          description: Method Not Allowed
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "409":
          description: User with this email already exists
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Register a new user
      tags:
      - Auth
  /api/v1/calculate:
    post:
      consumes:
      - application/json
      description: Calculates composite pricing across cloud providers for requested
        compute, storage, and network specs.
      parameters:
      - description: Composite workload request payload
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/rest.CalculateRequestBody'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.CalculateResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get composite pricing calculation
      tags:
      - calculator
  /api/v1/prices/compute:
    get:
      description: Returns normalized compute pricing across cloud providers for requested
        specs.
      parameters:
      - description: Requested vCPU count (e.g. 4)
        in: query
        name: vcpu
        type: number
      - description: Requested RAM in GB (e.g. 16)
        in: query
        name: ram_gb
        type: number
      - description: Instance family filter (e.g. t3, c5)
        in: query
        name: family
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      - description: 'Strict instance family matching (default: true)'
        in: query
        name: strict_family
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.ComputeComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get compute pricing comparison
      tags:
      - prices
  /api/v1/prices/network:
    get:
      description: Returns normalized network / data transfer egress pricing across
        cloud providers (AWS, Azure, GCP) for requested specs.
      parameters:
      - description: Requested network egress in GB (e.g. 500, max 10000000)
        in: query
        name: egress_gb
        type: string
      - description: Canonical transfer type (intra_region, inter_region, internet_egress)
        in: query
        name: transfer_type
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.NetworkComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get network pricing comparison
      tags:
      - prices
  /api/v1/prices/storage:
    get:
      description: Returns normalized storage pricing across cloud providers (AWS,
        Azure, GCP) for requested specs.
      parameters:
      - description: Requested storage size in GB (e.g. 500, max 1000000)
        in: query
        name: size_gb
        type: string
      - description: Requested canonical storage class (e.g. standard, infrequent_access,
          archive)
        in: query
        name: storage_class
        type: string
      - description: 'Canonical region group (default: us-east)'
        in: query
        name: region
        type: string
      - description: 'Target currency code (default: USD)'
        in: query
        name: currency
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/rest.StorageComparisonResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/middleware.RFC7807Error'
      summary: Get storage pricing comparison
      tags:
      - prices
swagger: "2.0"
